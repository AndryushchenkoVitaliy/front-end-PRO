'use strict';

const str1 = prompt("Введите первую строку:");
const str2 = prompt("Введите вторую строку:");
const str3 = prompt("Введите третью строку:");

// Произвольный порядок (можно менять местами)
console.log(`${str2}, ${str3}, ${str1}`);